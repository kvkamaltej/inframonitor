fastapi==0.115.6
uvicorn==0.34.0
# Required for the interactive shell: plain uvicorn ships no websocket protocol
# implementation, so WS upgrades are rejected with 404. uvicorn[standard] would bundle
# this, but it also pulls uvloop/httptools/watchfiles which the lite profile does not want.
websockets==17.0.1
sqlalchemy==2.0.36
pydantic-settings==2.7.1
python-jose[cryptography]==3.3.0
cryptography==44.0.2
passlib[bcrypt]==1.7.4
bcrypt==4.0.1
httpx==0.28.1
paramiko==3.5.1
# HashiCorp Vault client for the optional Vault secrets backend (see app/services/secrets.py).
# Only used when Vault is enabled in app settings; the default Fernet path pulls nothing from it.
hvac==2.3.0
psycopg[binary]==3.2.3
# Database console (feature/db-connect): MySQL driver. psycopg above already covers PostgreSQL.
pymysql==1.1.1
# SQL Server driver for the mssql engine. Imported lazily inside app/services/db_console.py, so
# the module imports without it; its manylinux wheels bundle FreeTDS, so no system libraries are
# needed at runtime.
pymssql==2.3.2
# Kubernetes clusters (feature/kubernetes): official client for the live cluster reads/actions.
# Imported lazily inside app/services/kube.py, so the lite profile only needs it when the feature
# is used; PyYAML (already pulled in) parses pasted kubeconfig YAML.
kubernetes==31.0.0
# Inventory export to Excel (GET /api/servers/export.xlsx). Pure-python wheel, no system
# libraries; imported lazily in the route so the app still starts without it.
openpyxl==3.1.5
prometheus-client==0.21.1
python-multipart==0.0.20
pytest==8.3.4
